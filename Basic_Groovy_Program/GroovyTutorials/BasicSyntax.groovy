/**
here we are going to write the basic syntax progrm
*/
class BasicSyntax{
    static void main(String[] args) { 
        
        //to print the hello world
        println('Hello world');

        //to declare the identifier we are using the def keyword e.g below(it may be variables as well as method or function)
        def a = 5; 

        //to print the identifier
        println(a);
    }
}